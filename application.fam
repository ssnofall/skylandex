App(
    appid="skylandex",
    name="Skylandex",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="skylandex_app",
    requires=["gui", "nfc", "storage", "notification"],
    stack_size=8 * 1024,
    fap_category="NFC",
    fap_version=(1, 0),
    fap_icon="skylandex.png",  # 10x10 1-bit PNG
    fap_description="Skylander NFC scanner, collection index, and emulator",
    fap_author="Snofall",
    fap_weburl="https://github.com/ssnofall/skylandex",
    fap_file_assets="resources",
)